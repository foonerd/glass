[package]
name = "expose"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "A Scene and assets become one RGBA frame"

[dependencies]
lead = { path = "../lead" }
plot = { path = "../plot" }
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
font8x8 = "0.3"
ab_glyph = "0.2.32"
