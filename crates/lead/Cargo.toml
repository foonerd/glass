[package]
name = "lead"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Typed values on the leads between stations"

[dependencies]
serde = { version = "1", features = ["derive"] }
