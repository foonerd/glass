[package]
name = "glasstap"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "The audio tap: an ALSA scope that measures the stream and shares it with the display"
build = "build.rs"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
tap = { path = "../../crates/tap" }
libc = "0.2"

[lints]
workspace = true
