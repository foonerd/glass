[package]
name = "glass-serve"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "The player's end of the line: the tap's frames to remote displays over UDP"

[[bin]]
name = "glass-serve"
path = "src/main.rs"

[dependencies]
tap = { path = "../../crates/tap" }
serde_json = "1"

[lints]
workspace = true
